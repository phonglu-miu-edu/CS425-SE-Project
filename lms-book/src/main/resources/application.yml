server:
  port: 8084
spring:
  cloud:
    bootstrap:
      enabled: true
  application:
    name: BookService
eureka:
  client:
    service-url:
      defaultZone:
        http://localhost:8761/eureka